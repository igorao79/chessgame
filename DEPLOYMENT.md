# 🚀 Деплой на Render.com

## Почему Render?

- ✅ **Бесплатный тариф** - достаточно для начала
- ✅ **Поддержка WebSocket** - необходимо для Socket.io
- ✅ **Встроенный Redis** - для сохранения игр
- ✅ **Автоматический деплой** - из GitHub
- ✅ **SSL сертификат** - автоматически

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка проекта

Убедитесь, что файл `package.json` содержит правильные скрипты:

```json
{
  "scripts": {
    "dev": "node server.js",
    "build": "next build",
    "start": "NODE_ENV=production node server.js"
  }
}
```

✅ Уже настроено в вашем проекте!

### Шаг 2: Создание репозитория на GitHub

1. Создайте новый репозиторий на [GitHub](https://github.com/new)
2. Назовите его, например: `chess-game`
3. Выполните команды в папке проекта:

```bash
git init
git add .
git commit -m "Initial commit: Chess game with AI and multiplayer"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/chess-game.git
git push -u origin main
```

### Шаг 3: Регистрация на Render

1. Перейдите на [render.com](https://render.com)
2. Нажмите **"Get Started for Free"**
3. Войдите через GitHub

### Шаг 4: Создание Redis instance (опционально, но рекомендуется)

1. В dashboard Render нажмите **"New +"**
2. Выберите **"Redis"**
3. Настройте:
   - **Name**: `chess-redis`
   - **Plan**: `Free` (25 MB, достаточно для начала)
   - Нажмите **"Create Redis"**
4. **Сохраните Internal Redis URL** - он понадобится позже

### Шаг 5: Создание Web Service

1. В dashboard Render нажмите **"New +"**
2. Выберите **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Настройте:

#### Основные настройки:
- **Name**: `chess-game` (или любое другое имя)
- **Region**: выберите ближайший к вам регион
- **Branch**: `main`
- **Runtime**: `Node`
- **Build Command**: `npm install && npm run build`
- **Start Command**: `npm start`

#### План:
- **Instance Type**: `Free` (для начала)
  - ⚠️ **Важно**: На бесплатном плане сервис "засыпает" после 15 минут неактивности
  - Первый запрос после "сна" может занять 30-60 секунд

### Шаг 6: Настройка переменных окружения

В разделе **Environment Variables** добавьте:

```
# Если создали Redis на Render:
REDIS_HOST=<ваш-redis-internal-host>
REDIS_PORT=6379
REDIS_PASSWORD=<ваш-redis-password>

# URL вашего приложения (заполните после первого деплоя)
NEXT_PUBLIC_SOCKET_URL=https://ваше-приложение.onrender.com

# Production mode
NODE_ENV=production
```

**Где взять Redis данные?**
- Откройте ваш Redis instance на Render
- Скопируйте **Internal Redis URL**
- Формат: `redis://red-xxxxx:password@internal-host:6379`
- Разделите на: host, password

### Шаг 7: Деплой

1. Нажмите **"Create Web Service"**
2. Render автоматически начнет деплой
3. Процесс займет 3-5 минут
4. После завершения вы увидите URL вашего приложения

### Шаг 8: Обновление NEXT_PUBLIC_SOCKET_URL

1. Скопируйте URL вашего приложения (например: `https://chess-game-abc123.onrender.com`)
2. В настройках Web Service обновите переменную:
   ```
   NEXT_PUBLIC_SOCKET_URL=https://chess-game-abc123.onrender.com
   ```
3. Сохраните - Render автоматически перезапустит приложение

## 🎮 Готово! Теперь можно играть онлайн

Ваше приложение доступно по адресу:
```
https://ваше-приложение.onrender.com
```

Поделитесь этой ссылкой с друзьями для онлайн игры!

## 🔄 Автоматические обновления

Теперь при каждом push в GitHub:
1. Render автоматически обнаружит изменения
2. Запустит новую сборку
3. Задеплоит обновленную версию

```bash
# Для обновления приложения:
git add .
git commit -m "Описание изменений"
git push
```

## 📊 Мониторинг

В dashboard Render вы можете:
- Смотреть логи в реальном времени
- Проверять статус приложения
- Видеть использование ресурсов
- Настроить алерты

## ⚠️ Ограничения бесплатного плана

### Web Service (Free):
- 750 часов в месяц (достаточно для одного сервиса)
- Автоматическое "засыпание" после 15 минут неактивности
- 512 MB RAM
- 0.1 CPU

### Redis (Free):
- 25 MB хранилища
- Достаточно для ~1000 активных игр

### Когда стоит перейти на платный план?

Рассмотрите платный план ($7/месяц), если:
- Много игроков одновременно
- Нужна постоянная доступность (без "засыпания")
- Требуется больше памяти для Redis

## 🔧 Альтернативные варианты деплоя

### Vercel + отдельный Socket.io сервер
**Минусы**: 
- Нужно два отдельных сервиса
- Vercel не поддерживает WebSocket в serverless functions
- Сложнее настроить

### Railway
**Плюсы**:
- Похож на Render
- $5 бесплатно каждый месяц
- Хорошая производительность

**Минусы**:
- Требуется привязка карты

### Heroku
**Плюсы**:
- Стабильная платформа
- Много документации

**Минусы**:
- Нет бесплатного плана (от $7/месяц)

## 🐛 Устранение проблем

### Приложение не запускается

1. Проверьте логи в Render dashboard
2. Убедитесь, что `npm start` работает локально:
   ```bash
   npm run build
   NODE_ENV=production npm start
   ```

### Socket.io не подключается

1. Проверьте `NEXT_PUBLIC_SOCKET_URL` - должен быть HTTPS
2. Убедитесь, что URL правильный (без "/" в конце)
3. Проверьте логи на наличие ошибок CORS

### Redis connection error

1. Проверьте, что Redis instance запущен
2. Проверьте правильность host, port, password
3. Используйте **Internal URL**, а не External

### Приложение медленно запускается

Это нормально для бесплатного плана:
- Первый запрос после "сна" занимает 30-60 секунд
- Последующие запросы будут быстрыми
- Решение: платный план ($7/месяц) без "засыпания"

## 💡 Советы по оптимизации

### 1. Кеширование
Приложение уже настроено для кеширования статических ресурсов

### 2. Минимизация холодного старта
На бесплатном плане - неизбежен. На платном - отсутствует.

### 3. Мониторинг Redis
Следите за использованием памяти в Render dashboard

### 4. Логирование
Добавьте более подробное логирование для отладки:

```javascript
// В server.js уже есть логи подключений
console.log('Client connected:', socket.id);
```

## 📱 Мобильная версия

Приложение адаптивно и работает на всех устройствах:
- 📱 Телефоны
- 📱 Планшеты  
- 💻 Десктопы

## 🎯 Следующие шаги

После успешного деплоя вы можете:
1. ✅ Играть с друзьями онлайн
2. ✅ Делиться ссылкой в соцсетях
3. ✅ Добавить custom domain (на платном плане)
4. ✅ Настроить аналитику
5. ✅ Добавить новые функции

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Render dashboard
2. Изучите [документацию Render](https://render.com/docs)
3. Проверьте [GitHub Issues](https://github.com/render-examples)

---

**Готово! Ваше шахматное приложение теперь доступно всему миру! 🎉**

Удачи в онлайн партиях! ♟️

